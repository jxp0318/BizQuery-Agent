# BizQuery Agent 代码可读性与注释规范

本文件适用于整个仓库。后续新增、修改和重构代码时，默认遵守以下规则，无需用户重复提醒。

## 总体目标

代码和注释应让维护者在重新打开项目时，能够快速判断：

1. 这段代码在整个 Agent / RAG / NL2SQL 流程中负责什么。
2. 为什么选择当前实现，而不是只知道代码执行了什么。
3. 输入、输出、状态变化、异常边界和外部依赖分别是什么。

注释追求有效信息，不以数量为目标。简单赋值、显而易见的条件判断、循环和返回语句不添加逐行翻译式注释。

## 语言与命名

- 注释、docstring 和面向项目维护者的技术说明统一使用中文。
- 变量名、函数名、类名、类型名和模块名继续使用规范英文命名。
- 保留行业通用英文术语，例如 LangGraph、State、Checkpointer、RAG、SSE、SQL 和 ContextVar；首次出现复杂术语时说明其在当前流程中的含义。

## 模块与公开接口

- 重要模块在文件开头添加模块 docstring，说明模块职责、所处流程和主要边界。
- 重要类、公开函数和公开方法优先使用 docstring，至少说明：
  - 业务职责以及它属于流程的哪一步。
  - 关键输入参数的业务含义。
  - 返回值或产出事件的含义。
  - 重要副作用，例如写数据库、更新 Graph State、发送 SSE 或访问外部服务。
- Python 公开接口采用 `Args`、`Returns`、必要时 `Raises` 的结构；特别简单且类型与函数名已经足够清楚的接口可以使用精简 docstring。
- TypeScript/React 中复杂的公共 hook、数据转换函数和状态协调函数使用中文 JSDoc；简单组件属性不重复类型声明。

## 重要变量与数据字段

- ORM Model、Pydantic Schema、dataclass、TypedDict、LangGraph State 和 TypeScript 业务类型中的重要字段，应说明它代表的业务数据、由谁写入以及主要消费者。
- 对生命周期较长或容易混淆的成员变量与页面状态，说明其有效范围，例如“请求级 Session”“应用级 Redis Saver”“当前 SSE 请求控制器”。
- 多存储项目中的字段应注明归属和边界，例如完整查询结果属于 MySQL 历史，Redis 仅保留可重建摘要。
- 对框架约定字段说明框架语义，例如 SQLAlchemy 的 `__tablename__`、LangGraph 的 `thread_id` 和消息 reducer。
- 多个同类局部变量可以在代码块上方统一说明，不需要给每一行重复相同信息。
- `item`、`response`、`event` 等短作用域且含义明确的普通变量不强制注释，避免注释密度掩盖主流程。

示例：

```python
async def retrieve_context(query: str) -> list:
    """
    根据用户问题检索生成 SQL 所需的 Schema 和业务指标信息。

    Args:
        query: 用户输入的自然语言问题。

    Returns:
        检索、去重并融合后的上下文列表，供 SQL 生成节点消费。
    """
```

## 必须重点解释的代码

以下代码只要存在不明显的设计选择，就应在代码块上方或对应公开接口 docstring 中解释“为什么”：

- Agent 工作流节点及其在整张图中的前后关系。
- LangGraph State 字段、reducer、条件边和 Checkpointer 状态流转。
- RAG 的关键词抽取、多路召回、去重、融合、过滤和降级策略。
- 数据库查询、事务边界、读写顺序、索引使用和一致性处理。
- SQL 生成、校验、纠错、`EXPLAIN` 与最终执行之间的安全边界。
- 异常处理、补偿操作、幂等、重试、超时和降级逻辑。
- 并发、异步任务、锁、协程取消和资源释放。
- ContextVar、SSE、WebSocket、流式事件与跨请求上下文传播。
- MySQL、Redis、Qdrant、Elasticsearch 等多存储之间的职责划分。

复杂代码块应先说明整体思路，再进入实现。例如：

```python
# 字段、指标和字段真实值使用不同检索后端，三路任务不存在数据依赖，
# 因此并行执行后再统一合并，避免串行网络请求放大问数链路延迟。
results = await asyncio.gather(
    field_retrieval(),
    metric_retrieval(),
    value_retrieval(),
)
```

## 注释内容要求

优先解释以下信息：

- 设计原因：为什么选择这种结构、顺序或数据边界。
- 流程作用：当前逻辑的上游输入和下游消费者是谁。
- 技术取舍：替代方案是什么，当前方案解决了什么问题，又保留了什么限制。
- 风险控制：为什么需要校验、锁、超时、幂等、回滚或补偿。
- 非显然行为：框架自动完成了什么，哪些状态会跨请求或跨进程保留。

避免以下无效注释：

```python
# 判断 result 是否为空
if not result:
    ...
```

应改为说明业务原因：

```python
# 检索结果为空时不继续调用 LLM，避免模型在缺少真实 Schema 约束时生成不可靠 SQL。
if not result:
    ...
```

## 修改与重构要求

- 保留已有且仍与实现一致的有价值注释。
- 逻辑、参数、返回值、调用顺序或异常边界变化时，同步更新相关注释和 docstring。
- 删除已经失效、与代码矛盾或仅重复代码字面含义的旧注释。
- 移动逻辑时，同时移动或重写解释其设计原因的注释，避免注释脱离对应代码块。
- 引入新抽象时，说明它解决的流程问题；不要只写“工具类”“公共方法”等空泛描述。
- 修复缺陷时，如果根因不明显，在关键修复点解释原行为为什么会失败，以及当前约束如何避免复发。

## Agent / RAG / LangGraph 项目特别约束

- Graph 节点 docstring 应说明读取哪些 State、写回哪些 State、发出哪些流式事件以及失败时如何处理。
- State 字段注释应区分“跨轮短期记忆”和“单轮临时状态”，避免把大体量召回结果误放入 Checkpoint。
- 检索代码应说明检索对象、检索后端、合并时机和结果如何约束 SQL 生成。
- 多存储代码应明确事实来源和可重建数据。例如 MySQL 是历史事实来源、Redis Checkpoint 是可恢复的 Runtime State。
- Prompt 或模型调用前应说明输入上下文为何足够、哪些数据被主动排除，以及失败时的降级路径。
- SSE 代码应说明事件类型、写入时机，以及为什么流开始后异常需要包装成事件而不能再修改 HTTP 状态码。

## 提交前自检

完成代码改动后检查：

- 重要新增模块、类和公开函数是否有中文 docstring。
- 关键业务块是否解释了设计原因和流程作用。
- 是否出现逐行翻译代码的噪声注释。
- 注释是否与当前参数、返回值、状态字段和异常行为一致。
- 是否清楚标明当前实现边界，避免把规划中的能力写成已经实现。
- 阅读者能否仅沿代码与注释理解一次请求从入口到结果的主链路。
